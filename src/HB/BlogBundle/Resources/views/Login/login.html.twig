{# empty Twig template #}
{% if error %}
    <div>{{ error.message }}</div>
{% endif %}

<form class = "navbar-form-pull-right" action="{{ path('login_check') }}" method="post">
    <li><input type="text" id="username" name="_username" value="{{ last_username }}" /></li>
    <li> <input type="password" id="password" name="_password" /></li>

    {#
        Si vous voulez contrôler l'URL vers laquelle l'utilisateur est redirigé en cas de succès
        (plus de détails ci-dessous)
        <input type="hidden" name="_target_path" value="/account" />
    #}

    <button type="submit">login</button>
</form>